'use strict';

const dogage = document.querySelector('.dogage');
const humanage = document-querySelector('.humanage');

if (dogage = 1) {
    humanage.innerHTML = humanage.innerHTML + 15;
  } else if (dogage = 2) {
    humanage.innerHTML += 9;}